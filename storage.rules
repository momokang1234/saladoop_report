rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /reports/{uid}/{allPaths=**} {
      // 인증된 사용자만 본인의 경로에 업로드/다운로드 가능
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
